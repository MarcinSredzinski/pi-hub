namespace SensorDataStore.WebApi.Auth
{
    public class TokenService
    {
        private readonly TimeSpan _expiration = new TimeSpan(0, 30, 0);

        //internal string BuildToken(string key, string issuer, UserDto user)
        //{

        //}
    }
}
